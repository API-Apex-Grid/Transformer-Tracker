# Backend environment variable template
# Copy this file to .env (NOT committed) or export vars in your shell.
# Any values left blank must be filled before running.

# --- Database (Postgres / Supabase) ---
DB_URL=jdbc:postgresql://YOUR_DB_HOST:5432/postgres?sslmode=require
DB_USERNAME=postgres
DB_PASSWORD=CHANGE_ME

# Override JDBC driver if needed
DB_DRIVER=org.postgresql.Driver

# --- JWT / Security ---
APP_JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING_AT_LEAST_32_CHARS
APP_JWT_EXPIRY=86400

# --- Optional server overrides ---
SERVER_PORT=8080
